version: '3.8'
services:
  db:
    image: mysql:latest
    command: --allow-public-key-retrieval=true
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: repfogo
    ports:
      - "3307:3306"
    volumes:
      - ./data:/var/lib/mysql
